#!/usr/bin/env build

echo "cleaning: ${1}"

eval $(build query --environ "${1}")

# FIXME need a way to flag a path as NOCLEAN
if [ "${CATEGORY}" = "buildtools" ]; then
	rm -f "${W}/.installed"
else
	if [ -d "${W}" ]; then
		rm -rf "${W}"
	fi
fi

# vim: filetype=sh
