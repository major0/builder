#!/usr/bin/env build

echo "cleaning: ${1}"

eval $(build query --environ "${1}")

rm -f "${W}/.installed"
if [ -z "${TOOLCHAIN}" ]; then
	if [ -d "${W}" ]; then
		rm -rf "${W}"
	fi
fi

# vim: filetype=sh
