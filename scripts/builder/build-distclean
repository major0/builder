#!/usr/bin/env build

echo "dist-cleaning: ${1}"

for dir in "$(build query --workdir "${1}")" "$(build query --destdir "${1}")"; do
	if [ -d "${dir}" ]; then
		rm -rf "${dir}" &
	fi
done

wait

# vim: filetype=sh
