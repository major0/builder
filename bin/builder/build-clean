#!/usr/bin/env build

echo "cleaning: ${1}"
import "${1}"

if [ ! -z "${TOOLCHAIN}" ]; then
	rm -f "${L}/.installed"
	exit 0
fi

for dir in "${L}" "${W}" "${D}"; do
	if [ -d "${dir}" ]; then
		rm -rf "${dir}" &
	fi
done

wait

# vim: filetype=sh
